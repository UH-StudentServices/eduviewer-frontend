---

ssl_cert_path: "/etc/ssl/hy-certs/{{ ssl_cert_file }}"
ssl_key_path: "/etc/ssl/private/{{ ssl_key_file }}"
ssl_cert_bundle_path: "/etc/ssl/hy-certs/{{ ssl_cert_bundle_file }}"
ca_bundle_path: "/etc/ssl/terena-bundle.pem"
